---
phase: 01-secure-onboarding-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - apps/frontend/src/lib/api.ts
  - apps/frontend/src/components/OnboardingForm.tsx
  - apps/backend/tests/conftest.py
  - apps/backend/tests/test_onboarding_api.py
  - apps/backend/tests/test_upload_api.py
  - apps/backend/tests/test_widget_snippet.py
  - apps/backend/tests/test_tenant_authz.py
  - apps/backend/tests/test_cors_policy.py
  - apps/backend/tests/test_rate_limits.py
autonomous: true
requirements:
  - ONB-01
  - ONB-02
  - ONB-03
  - ONB-04
  - SEC-01
  - SEC-02
  - SEC-03
must_haves:
  truths:
    - "Owner sees field-level onboarding errors instead of generic failure copy."
    - "Owner can copy a tenant-specific widget snippet from onboarding completion UI."
    - "Phase 1 requirements are covered by runnable automated tests."
  artifacts:
    - path: "apps/frontend/src/components/OnboardingForm.tsx"
      provides: "Actionable onboarding error display + widget snippet handoff"
    - path: "apps/frontend/src/lib/api.ts"
      provides: "Structured API error parsing for frontend feedback"
    - path: "apps/backend/tests/test_rate_limits.py"
      provides: "SEC-03 automated proof"
  key_links:
    - from: "apps/frontend/src/lib/api.ts"
      to: "apps/frontend/src/components/OnboardingForm.tsx"
      via: "structured error objects mapped to field-level UI"
      pattern: "fieldErrors"
    - from: "apps/backend/tests/test_tenant_authz.py"
      to: "apps/backend/app/auth/dependencies.py"
      via: "401/403 assertions for owner access"
      pattern: "status_code == 403"
    - from: "apps/backend/tests/test_cors_policy.py"
      to: "apps/backend/app/main.py"
      via: "origin allowlist assertions"
      pattern: "access-control-allow-origin"
---

<objective>
Deliver onboarding UX clarity and complete Nyquist verification coverage for all Phase 1 requirements.

Purpose: Convert backend contract changes into clear owner feedback and lock in automated regression protection for ONB-01..ONB-04 and SEC-01..SEC-03.
Output: Updated onboarding UI/API error parsing and a full Phase 1 backend test suite.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-secure-onboarding-foundation/01-UI-SPEC.md
@.planning/phases/01-secure-onboarding-foundation/01-VALIDATION.md
@apps/frontend/src/lib/api.ts
@apps/frontend/src/components/OnboardingForm.tsx
@apps/backend/tests/test_health.py

<interfaces>
From apps/frontend/src/lib/api.ts:
```ts
export async function createTenant(payload: TenantCreatePayload): Promise<TenantCreateResponse> {
  const response = await fetch(`${API_BASE}/tenants`, { ... });
  if (!response.ok) {
    throw new Error("Failed to create tenant");
  }
  return response.json();
}
```

From apps/frontend/src/components/OnboardingForm.tsx:
```tsx
catch (submissionError) {
  setError("Could not create assistant. Check backend API and try again.");
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Surface actionable onboarding + upload errors and snippet handoff UI (ONB-01 to ONB-04, per D-02/D-03/D-04)</name>
  <read_first>
    - apps/frontend/src/lib/api.ts
    - apps/frontend/src/components/OnboardingForm.tsx
    - .planning/phases/01-secure-onboarding-foundation/01-UI-SPEC.md
  </read_first>
  <files>apps/frontend/src/lib/api.ts, apps/frontend/src/components/OnboardingForm.tsx</files>
  <action>In api.ts implement structured error parsing for 400/401/403/409/413/429 responses and expose typed errors with fields `{ message: string; fieldErrors?: Record<string,string>; retryAfterSeconds?: number }`. In OnboardingForm.tsx replace generic submission error copy with field-level rendering for business_name/domain/services and deterministic upload error messaging per D-02. Add a post-success snippet panel showing `tenant.widget_embed_script` in a read-only textarea plus a copy button labeled `Create Tenant Profile` flow output per UI-SPEC copy direction; keep onboarding CTA text exactly `Create Tenant Profile`.</action>
  <acceptance_criteria>
    - `apps/frontend/src/lib/api.ts` defines a typed error shape containing `fieldErrors` and `retryAfterSeconds`.
    - `apps/frontend/src/components/OnboardingForm.tsx` contains button text `Create Tenant Profile`.
    - `apps/frontend/src/components/OnboardingForm.tsx` contains UI text `No onboarding documents yet` and a visible snippet textarea bound to `widget_embed_script`.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix apps/frontend run build</automated>
  </verify>
  <done>Owners get concrete correction guidance for onboarding/upload failures and can copy a tenant-specific widget snippet directly after creation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Wave 0/Phase 1 backend verification suite for ONB and SEC requirements</name>
  <read_first>
    - apps/backend/tests/test_health.py
    - .planning/phases/01-secure-onboarding-foundation/01-RESEARCH.md
    - .planning/phases/01-secure-onboarding-foundation/01-VALIDATION.md
  </read_first>
  <files>apps/backend/tests/conftest.py, apps/backend/tests/test_onboarding_api.py, apps/backend/tests/test_upload_api.py, apps/backend/tests/test_widget_snippet.py, apps/backend/tests/test_tenant_authz.py, apps/backend/tests/test_cors_policy.py, apps/backend/tests/test_rate_limits.py</files>
  <behavior>
    - Test 1: `test_create_tenant_required_fields` verifies missing/invalid onboarding inputs fail with field-level errors.
    - Test 2: `test_upload_supported_files` and `test_upload_rejects_invalid_type_and_size` verify ONB-02/ONB-03 behavior.
    - Test 3: `test_widget_snippet_is_parameterized` verifies tenant-specific and defaulted embed script output.
    - Test 4: `test_cross_tenant_forbidden` verifies SEC-01 enforcement.
    - Test 5: `test_disallowed_origin_blocked` verifies SEC-02 CORS policy.
    - Test 6: `test_chat_and_upload_rate_limited` verifies SEC-03 deterministic 429 behavior.
  </behavior>
  <action>Create pytest fixtures in conftest.py for async test DB, tenant factory, and owner-token helper aligned with Plan 01 auth dependency. Add each missing test file named exactly as listed in the validation map from 01-RESEARCH.md. Use explicit endpoint paths `/api/tenants`, `/api/tenants/{tenant_id}/upload`, `/api/chat`, and `/api/analytics/tenants/{tenant_id}/stats`. For SEC-03 assertions, verify both `status_code == 429` and response JSON includes `retry_after_seconds` integer.</action>
  <acceptance_criteria>
    - All six test files exist under `apps/backend/tests/` with test function names matching the validation map.
    - `apps/backend/tests/test_tenant_authz.py` contains explicit assertions for 401 and 403.
    - `apps/backend/tests/test_rate_limits.py` asserts `retry_after_seconds` in 429 JSON payload.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest apps/backend/tests/test_onboarding_api.py apps/backend/tests/test_upload_api.py apps/backend/tests/test_widget_snippet.py apps/backend/tests/test_tenant_authz.py apps/backend/tests/test_cors_policy.py apps/backend/tests/test_rate_limits.py -q</automated>
  </verify>
  <done>All Phase 1 requirement behaviors have automated backend test coverage and pass deterministically.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run phase-level verification gate and record outcomes</name>
  <read_first>
    - .planning/phases/01-secure-onboarding-foundation/01-VALIDATION.md
    - apps/backend/tests/
  </read_first>
  <files>apps/backend/tests/</files>
  <action>Execute full backend suite for confidence and then run frontend build gate. If failures occur, fix tests or implementation in the same run and rerun until green. Update phase summary with exact commands and pass/fail outcomes to preserve reproducibility.</action>
  <acceptance_criteria>
    - Command output includes all targeted Phase 1 test files passing.
    - Frontend build command exits successfully.
    - Summary includes exact verify commands used and final pass state.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest apps/backend/tests -q; npm --prefix apps/frontend run build</automated>
  </verify>
  <done>Phase 1 execution has a reproducible green verification gate spanning backend behavior tests and frontend build integrity.</done>
</task>

</tasks>

<verification>
Primary phase checks:
- `python -m pytest apps/backend/tests -q`
- `npm --prefix apps/frontend run build`
</verification>

<success_criteria>
- Frontend onboarding provides actionable, field-level feedback and snippet copy handoff.
- Automated tests exist and pass for ONB-01, ONB-02, ONB-03, ONB-04, SEC-01, SEC-02, and SEC-03.
- Phase 1 has deterministic verification commands that can be rerun without manual interpretation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-onboarding-foundation/01-03-SUMMARY.md`.
</output>
