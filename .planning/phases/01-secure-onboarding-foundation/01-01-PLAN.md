---
phase: 01-secure-onboarding-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/core/config.py
  - apps/backend/app/main.py
  - apps/backend/app/auth/dependencies.py
  - apps/backend/app/api/tenants.py
  - apps/backend/app/api/analytics.py
autonomous: true
requirements:
  - SEC-01
  - SEC-02
must_haves:
  truths:
    - "Tenant-scoped owner operations require authenticated owner credentials, not only tenant_id."
    - "Cross-tenant attempts on owner endpoints fail with explicit authorization errors."
    - "Production CORS accepts only explicitly configured origins."
  artifacts:
    - path: "apps/backend/app/auth/dependencies.py"
      provides: "Owner authentication and tenant authorization dependency contracts"
    - path: "apps/backend/app/core/config.py"
      provides: "CORS allowlist and owner auth config values"
    - path: "apps/backend/app/main.py"
      provides: "Strict CORS middleware wiring"
  key_links:
    - from: "apps/backend/app/api/tenants.py"
      to: "apps/backend/app/auth/dependencies.py"
      via: "Depends(require_owner_tenant_access)"
      pattern: "require_owner_tenant_access"
    - from: "apps/backend/app/api/analytics.py"
      to: "apps/backend/app/auth/dependencies.py"
      via: "Depends(require_owner_tenant_access)"
      pattern: "require_owner_tenant_access"
    - from: "apps/backend/app/main.py"
      to: "apps/backend/app/core/config.py"
      via: "settings.allowed_cors_origins"
      pattern: "allowed_cors_origins"
---

<objective>
Implement tenant authorization and CORS hardening so owner-only endpoints enforce identity checks and browser-origin boundaries.

Purpose: Satisfy SEC-01 and SEC-02 before onboarding/upload/chat hardening layers are added.
Output: Auth dependency module, protected tenant owner routes, and explicit-origin CORS configuration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-secure-onboarding-foundation/1-CONTEXT.md
@.planning/phases/01-secure-onboarding-foundation/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@apps/backend/app/main.py
@apps/backend/app/api/tenants.py
@apps/backend/app/api/analytics.py
@apps/backend/app/core/config.py

<interfaces>
From apps/backend/app/main.py:
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

From apps/backend/app/api/tenants.py:
```python
@router.post("/{tenant_id}/knowledge")
async def ingest_knowledge(tenant_id: str, payload: KnowledgeIngestRequest, db: AsyncSession = Depends(get_db_session)) -> dict:
    return await assistant_service.ingest_knowledge(tenant_id, payload, db)

@router.post("/{tenant_id}/upload")
async def upload_document(tenant_id: str, file: UploadFile = File(...), db: AsyncSession = Depends(get_db_session)) -> dict:
```

From apps/backend/app/api/analytics.py:
```python
@router.get("/tenants/{tenant_id}/stats")
async def get_tenant_stats(tenant_id: str, db: AsyncSession = Depends(get_db_session)) -> dict:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add owner auth and tenant authorization dependency (SEC-01, per D-05 and D-06)</name>
  <read_first>
    - apps/backend/app/api/tenants.py
    - apps/backend/app/api/analytics.py
    - .planning/phases/01-secure-onboarding-foundation/1-CONTEXT.md
  </read_first>
  <files>apps/backend/app/auth/dependencies.py, apps/backend/app/core/config.py, apps/backend/app/api/tenants.py, apps/backend/app/api/analytics.py</files>
  <behavior>
    - Test 1: Requests to owner tenant endpoints without owner auth header return 401 with actionable detail.
    - Test 2: Requests with owner auth header for mismatched tenant_id return 403 with explicit cross-tenant denial.
  </behavior>
  <action>Create apps/backend/app/auth/dependencies.py with a single reusable dependency require_owner_tenant_access(tenant_id: str, x_owner_token: str | None) that validates header X-Owner-Token against an HMAC-SHA256 token derived from tenant_id and settings.owner_access_secret (constant-time compare). In apps/backend/app/core/config.py add owner_access_secret: str and owner_auth_header: str = "X-Owner-Token". Apply Depends(require_owner_tenant_access) to /tenants/{tenant_id}/knowledge, /tenants/{tenant_id}/upload, and all /analytics/tenants/{tenant_id} routes. Return HTTP 401 for missing token and HTTP 403 for invalid or cross-tenant token with explicit error detail per D-06.</action>
  <acceptance_criteria>
    - `apps/backend/app/auth/dependencies.py` contains `def require_owner_tenant_access` and `hmac.compare_digest`.
    - `apps/backend/app/api/tenants.py` contains `Depends(require_owner_tenant_access)` on both tenant_id owner routes.
    - `apps/backend/app/api/analytics.py` contains `Depends(require_owner_tenant_access)` on each tenant_id analytics route.
  </acceptance_criteria>
  <verify>
    <automated>MISSING - Wave 3 task will create `apps/backend/tests/test_tenant_authz.py` and run `python -m pytest apps/backend/tests/test_tenant_authz.py -q`.</automated>
  </verify>
  <done>Owner tenant endpoints are blocked unless valid tenant-bound credentials are provided, and cross-tenant misuse returns explicit 403 responses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace wildcard CORS with explicit origin allowlist (SEC-02)</name>
  <read_first>
    - apps/backend/app/main.py
    - apps/backend/app/core/config.py
    - .planning/phases/01-secure-onboarding-foundation/01-RESEARCH.md
  </read_first>
  <files>apps/backend/app/core/config.py, apps/backend/app/main.py</files>
  <behavior>
    - Test 1: Allowed origin preflight receives CORS allow headers.
    - Test 2: Disallowed origin does not receive allow-origin for credentialed requests.
  </behavior>
  <action>Add allowed_cors_origins: list[str] to settings with comma-delimited env parsing via AI_AGENT_ALLOWED_CORS_ORIGINS, defaulting to `http://localhost:5173` for dev only. In apps/backend/app/main.py wire CORSMiddleware with allow_origins=settings.allowed_cors_origins, allow_credentials=True, allow_methods=["GET","POST","OPTIONS"], allow_headers=["Authorization","Content-Type", settings.owner_auth_header]. Remove wildcard origin/method/header configuration.</action>
  <acceptance_criteria>
    - `apps/backend/app/main.py` no longer contains `allow_origins=["*"]`.
    - `apps/backend/app/main.py` contains `allow_origins=settings.allowed_cors_origins`.
    - `apps/backend/app/core/config.py` contains `allowed_cors_origins` and parses `AI_AGENT_ALLOWED_CORS_ORIGINS`.
  </acceptance_criteria>
  <verify>
    <automated>MISSING - Wave 3 task will create `apps/backend/tests/test_cors_policy.py` and run `python -m pytest apps/backend/tests/test_cors_policy.py -q`.</automated>
  </verify>
  <done>CORS behavior is allowlist-driven and production-safe, with explicit origins and headers only.</done>
</task>

</tasks>

<verification>
Run owner-auth and CORS policy integration tests after test scaffolding exists:
- `python -m pytest apps/backend/tests/test_tenant_authz.py -q`
- `python -m pytest apps/backend/tests/test_cors_policy.py -q`
</verification>

<success_criteria>
- Owner tenant endpoints require valid tenant-bound auth token and reject cross-tenant access.
- CORS is configured through explicit allowlisted origins and no wildcard origins remain in main app setup.
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-onboarding-foundation/01-01-SUMMARY.md`.
</output>
