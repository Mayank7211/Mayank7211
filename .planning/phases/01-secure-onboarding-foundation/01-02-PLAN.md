---
phase: 01-secure-onboarding-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/backend/app/models/schemas.py
  - apps/backend/app/services/assistant_service.py
  - apps/backend/app/api/tenants.py
  - apps/backend/app/api/chat.py
autonomous: true
requirements:
  - ONB-01
  - ONB-02
  - ONB-03
  - ONB-04
  - SEC-03
must_haves:
  truths:
    - "Tenant creation rejects invalid required business fields with actionable validation errors."
    - "Onboarding upload accepts only PDF/DOCX/TXT and rejects oversized files with clear error details."
    - "Tenant creation returns a tenant-specific widget snippet with safe defaults and explicit parameters."
    - "Chat and upload endpoints return deterministic 429 responses when throttled."
  artifacts:
    - path: "apps/backend/app/models/schemas.py"
      provides: "Strict onboarding request constraints"
    - path: "apps/backend/app/services/assistant_service.py"
      provides: "Parameterized widget snippet generation"
    - path: "apps/backend/app/api/tenants.py"
      provides: "Upload validation + upload rate limiting"
    - path: "apps/backend/app/api/chat.py"
      provides: "Chat endpoint rate limiting"
  key_links:
    - from: "apps/backend/app/models/schemas.py"
      to: "apps/backend/app/api/tenants.py"
      via: "Pydantic request validation on POST /tenants"
      pattern: "TenantCreateRequest"
    - from: "apps/backend/app/services/assistant_service.py"
      to: "apps/backend/app/models/schemas.py"
      via: "TenantCreateResponse.widget_embed_script"
      pattern: "widget_embed_script"
    - from: "apps/backend/app/api/chat.py"
      to: "apps/backend/app/auth/dependencies.py"
      via: "rate-limit dependency"
      pattern: "RateLimiter"
---

<objective>
Harden onboarding contracts and abuse guards so tenant setup responses and high-cost endpoints behave predictably under both normal and abusive traffic.

Purpose: Implement ONB-01..ONB-04 and SEC-03 contracts with deterministic server behavior aligned to locked decisions D-01, D-02, D-03, D-04, D-07, D-08.
Output: Strict onboarding schema constraints, parameterized widget snippet output, upload validation clarity, and explicit 429 guardrails.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-secure-onboarding-foundation/1-CONTEXT.md
@.planning/phases/01-secure-onboarding-foundation/01-RESEARCH.md
@.planning/phases/01-secure-onboarding-foundation/01-UI-SPEC.md
@apps/backend/app/models/schemas.py
@apps/backend/app/services/assistant_service.py
@apps/backend/app/api/tenants.py
@apps/backend/app/api/chat.py

<interfaces>
From apps/backend/app/models/schemas.py:
```python
class TenantCreateRequest(BaseModel):
    business_name: str = Field(min_length=2, max_length=120)
    domain: str = Field(min_length=3, max_length=255)
    category: str = Field(min_length=2, max_length=80)
    services: List[str] = Field(default_factory=list, max_length=50)
```

From apps/backend/app/services/assistant_service.py:
```python
widget_embed_script = (
    "<script src='https://cdn.your-app.com/widget.js' "
    f"data-tenant-id='{tenant.id}'></script>"
)
```

From apps/backend/app/api/tenants.py:
```python
allowed_types = ['.pdf', '.docx', '.txt']
max_size = 10 * 1024 * 1024
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce strict onboarding validation and parameterized widget snippet (ONB-01, ONB-04; per D-01 to D-04)</name>
  <read_first>
    - apps/backend/app/models/schemas.py
    - apps/backend/app/services/assistant_service.py
    - .planning/phases/01-secure-onboarding-foundation/1-CONTEXT.md
  </read_first>
  <files>apps/backend/app/models/schemas.py, apps/backend/app/services/assistant_service.py</files>
  <behavior>
    - Test 1: Tenant creation with empty services list fails validation with field-level detail.
    - Test 2: Successful tenant creation returns widget_embed_script containing tenant_id and explicit defaults.
  </behavior>
  <action>In TenantCreateRequest set services to require at least one item and normalize service values (trim + reject blank-only items). Keep required fields strict for business_name, domain, and category per D-01. In AssistantService.create_tenant, generate widget_embed_script with explicit safe defaults per D-03/D-04: data-tenant-id, data-theme="light", data-position="bottom-right", data-primary-color="#0f766e", and script src `https://cdn.your-app.com/widget.js`. Ensure validation errors bubble as actionable Pydantic field details to support D-02.</action>
  <acceptance_criteria>
    - `apps/backend/app/models/schemas.py` contains a services constraint with minimum item count of 1.
    - `apps/backend/app/services/assistant_service.py` widget snippet includes `data-theme='light'` and `data-position='bottom-right'`.
    - `apps/backend/app/services/assistant_service.py` widget snippet still includes `data-tenant-id`.
  </acceptance_criteria>
  <verify>
    <automated>MISSING - Wave 3 task will create `apps/backend/tests/test_onboarding_api.py` and `apps/backend/tests/test_widget_snippet.py`, then run both tests.</automated>
  </verify>
  <done>Tenant creation enforces v1 required inputs and returns a copy-paste-ready tenant-specific snippet with explicit defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic upload and chat abuse protection with explicit 429 contracts (ONB-02, ONB-03, SEC-03; per D-07 and D-08)</name>
  <read_first>
    - apps/backend/app/api/tenants.py
    - apps/backend/app/api/chat.py
    - .planning/phases/01-secure-onboarding-foundation/01-RESEARCH.md
  </read_first>
  <files>apps/backend/app/api/tenants.py, apps/backend/app/api/chat.py</files>
  <behavior>
    - Test 1: Upload rejects unsupported type and >10MB files with actionable 400/413 details.
    - Test 2: Repeated calls on chat/upload exceed configured threshold and return deterministic 429 with retry guidance.
  </behavior>
  <action>Integrate fastapi-limiter dependency on POST /api/chat and POST /api/tenants/{tenant_id}/upload with explicit limits: chat 20 requests/minute per IP+tenant key, upload 5 requests/minute per IP+tenant key. Add a shared 429 response payload shape `{ "detail": "Rate limit exceeded", "retry_after_seconds": <int> }`. Keep upload type whitelist exactly `.pdf,.docx,.txt` and max file size 10MB, returning explicit error messages with allowed values. Do not silently swallow parser exceptions; map known validation failures to 400/413 and throttles to 429.</action>
  <acceptance_criteria>
    - `apps/backend/app/api/chat.py` contains limiter dependency and a 429 error response detail string `Rate limit exceeded`.
    - `apps/backend/app/api/tenants.py` upload route contains limiter dependency and `retry_after_seconds` in 429 payload.
    - `apps/backend/app/api/tenants.py` still enforces `.pdf`, `.docx`, `.txt` and `10MB` limits with explicit error details.
  </acceptance_criteria>
  <verify>
    <automated>MISSING - Wave 3 task will create `apps/backend/tests/test_upload_api.py` and `apps/backend/tests/test_rate_limits.py`, then run both tests.</automated>
  </verify>
  <done>Upload and chat endpoints enforce clear validation and deterministic throttling behavior without silent over-consumption paths.</done>
</task>

</tasks>

<verification>
After implementation and test scaffolding:
- `python -m pytest apps/backend/tests/test_onboarding_api.py -q`
- `python -m pytest apps/backend/tests/test_widget_snippet.py -q`
- `python -m pytest apps/backend/tests/test_upload_api.py -q`
- `python -m pytest apps/backend/tests/test_rate_limits.py -q`
</verification>

<success_criteria>
- Backend onboarding validation enforces required fields and returns actionable field-level feedback.
- Widget snippet output is tenant-specific, parameterized, and safe-defaulted.
- Chat and upload endpoints enforce deterministic rate limits with explicit retry guidance.
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-onboarding-foundation/01-02-SUMMARY.md`.
</output>
