---
phase: 02-tenant-aware-chat-baseline
plan: 01
type: execute
wave: 1
depends_on:
  - 01-00
files_modified:
  - apps/backend/app/services/assistant_service.py
  - apps/backend/app/api/chat.py
  - apps/backend/app/services/knowledge.py
  - apps/backend/app/models/schemas.py
  - apps/frontend/src/components/ChatSandbox.tsx
  - apps/frontend/src/components/ChatWidget.tsx
  - apps/backend/tests/test_chat_api.py
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
must_haves:
  truths:
    - "Visitors can open the widget and exchange messages with successful request/response flow."
    - "Assistant responses reflect tenant-specific context supplied by uploaded knowledge."
    - "Short conversation context is preserved across messages in the same session."
  artifacts:
    - path: "apps/backend/app/api/chat.py"
      provides: "Tenant-aware chat endpoint with session handling"
    - path: "apps/backend/tests/test_chat_api.py"
      provides: "Wave 0 tests for chat routing, session continuity, and tenant scoping"
---

<objective>
Implement tenant-aware chat baseline: reliable chat endpoint, session continuity (short-term), context loading, and test scaffolding to validate tenant scoping and reservation-detection behavior.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chat endpoint hardening and session continuity</name>
  <read_first>
    - apps/backend/app/services/assistant_service.py
    - apps/backend/app/api/chat.py
    - apps/backend/app/db/session.py
  </read_first>
  <files>apps/backend/app/api/chat.py, apps/backend/app/services/assistant_service.py</files>
  <action>
    Ensure the chat route enforces tenant existence and owner/tenant boundaries where appropriate; persist short session messages (in DB or ephemeral store) and load last N exchanges for context. Add explicit 429 handling hooks already present.
  </action>
  <acceptance_criteria>
    - Chat endpoint returns 200 for valid tenant messages and 404 for unknown tenant.
    - Chat endpoint includes context from recent session messages in model request.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Context retrieval integration</name>
  <read_first>
    - apps/backend/app/services/knowledge.py
    - apps/backend/app/services/model_gateway.py
  </read_first>
  <files>apps/backend/app/services/knowledge.py, apps/backend/app/services/model_gateway.py</files>
  <action>
    Wire a simple retrieval: for v1 use recent-chunks ranking (already available) and keep a clear extension point for semantic retrieval (pgvector) later. Ensure retrieval returns top-K blocks for `AssistantService._load_context`.
  </action>
  <acceptance_criteria>
    - `_load_context` returns a non-empty list when tenant seed knowledge exists.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reservation detection and persistence hook</name>
  <read_first>
    - apps/backend/app/services/assistant_service.py
    - apps/backend/app/models/entities.py
  </read_first>
  <files>apps/backend/app/services/assistant_service.py, apps/backend/app/models/entities.py</files>
  <action>
    Ensure reservation detection sets a `ReservationEntity` when detected and returns `reservation_detected` flag in `ChatResponse`. Add unit tests validating detection for sample dialogs.
  </action>
  <acceptance_criteria>
    - A matching `ReservationEntity` is created for reservation-like messages in tests.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Frontend widget chat integration</name>
  <read_first>
    - apps/frontend/src/components/ChatSandbox.tsx
    - apps/frontend/src/components/ChatWidget.tsx
  </read_first>
  <files>apps/frontend/src/components/ChatSandbox.tsx, apps/frontend/src/components/ChatWidget.tsx</files>
  <action>
    Add a minimal chat UI that posts to `/api/chat`, shows typing state, handles 429 with retry hint, and surfaces `reservation_detected` and confidence when present.
  </action>
  <acceptance_criteria>
    - Chat UI sends tenant_id and message and renders responses.
    - UI shows retry guidance when `retry_after_seconds` is returned.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Wave 0 tests for chat</name>
  <read_first>
    - apps/backend/tests/conftest.py
  </read_first>
  <files>apps/backend/tests/test_chat_api.py</files>
  <action>
    Create `tests/test_chat_api.py` with fixtures that mock the model gateway and verify tenant scoping, session continuity, reservation detection, and rate limit behavior.
  </action>
  <acceptance_criteria>
    - `pytest apps/backend/tests/test_chat_api.py` passes with mocked gateway.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run the backend chat tests and frontend build:

```
python -m pytest apps/backend/tests/test_chat_api.py -q
npm --prefix apps/frontend run build
```

</verification>

<output>
After completion create `.planning/phases/02-tenant-aware-chat-baseline/01-01-SUMMARY.md`.
</output>
