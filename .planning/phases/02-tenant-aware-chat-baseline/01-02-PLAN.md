---
phase: 02-tenant-aware-chat-baseline
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - apps/backend/app/services/knowledge.py
  - apps/backend/app/services/model_gateway.py
  - apps/backend/app/services/assistant_service.py
  - apps/backend/tests/test_knowledge_retrieval.py
autonomous: true
requirements:
  - RETR-01
  - RETR-02
must_haves:
  truths:
    - "Retrieval returns semantically relevant context for tenant queries."
    - "Switching between token-overlap and vector retrieval is configurable."
  artifacts:
    - path: "apps/backend/app/services/knowledge.py"
      provides: "Pluggable retrieval with a vector-store adapter"
    - path: "apps/backend/tests/test_knowledge_retrieval.py"
      provides: "Unit tests validating retrieval behavior"
---

<objective>
Implement semantic retrieval and make retrieval pluggable so Phase 2 can evolve to pgvector-based ranking, while keeping a simple fallback (token overlap) for SQLite / dev setups.
</objective>

<tasks>

<task type="manual" tdd="true">
  <name>Task 1: Design a VectorStoreAdapter interface</name>
  <read_first>
    - apps/backend/app/services/knowledge.py
  </read_first>
  <action>
    Add an adapter interface that supports `index_documents(tenant_id, blocks)` and `query(tenant_id, query, top_k)` with a simple in-memory implementation and a pgvector-backed implementation placeholder.
  </action>
  <acceptance_criteria>
    - In-memory adapter returns expected top-K by simple cosine or token-overlap fallback.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pgvector integration (optional for production)</name>
  <read_first>
    - apps/backend/app/services/knowledge.py
    - apps/backend/app/core/config.py
  </read_first>
  <action>
    Implement a `PgVectorAdapter` that uses raw SQL to call pgvector similarity search if `DATABASE_URL` indicates Postgres and `AI_AGENT_ENABLE_PGVECTOR=true`. Add migrations notes.
  </action>
  <acceptance_criteria>
    - `PgVectorAdapter.query` returns candidates when run against a Postgres + pgvector DB (manual verification may be required).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests for retrieval behavior</name>
  <read_first>
    - apps/backend/tests/conftest.py
  </read_first>
  <action>
    Create `apps/backend/tests/test_knowledge_retrieval.py` to validate both token-overlap fallback and the in-memory adapter. Ensure CI covers retrieval logic via unit tests.
  </action>
  <acceptance_criteria>
    - Unit tests pass in the repo's CI (local run: `pytest apps/backend/tests/test_knowledge_retrieval.py`).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire retrieval toggle in AssistantService</name>
  <read_first>
    - apps/backend/app/services/assistant_service.py
    - apps/backend/app/core/config.py
  </read_first>
  <action>
    Make retrieval use the configured adapter; expose `AI_AGENT_ENABLE_PGVECTOR` and `AI_AGENT_RETRIEVAL_K` settings and ensure `_load_context` respects them.
  </action>
  <acceptance_criteria>
    - `_load_context` uses the active adapter and returns top-K chunks.
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 5: Document migration and rollout notes</name>
  <action>
    Add `.planning/phases/02-tenant-aware-chat-baseline/01-02-SUMMARY.md` and update README with optional Postgres+pgvector setup instructions.
  </action>
  <acceptance_criteria>
    - README contains steps to enable pgvector and index creation.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run unit tests for retrieval and build the project.

```
python -m pytest apps/backend/tests/test_knowledge_retrieval.py -q
npm --prefix apps/frontend run build
```

</verification>

<output>
Create `.planning/phases/02-tenant-aware-chat-baseline/01-02-SUMMARY.md` after completion.
</output>
